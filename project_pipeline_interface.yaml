pipeline_name: NGS.Repeats
pipeline_type: project
output_schema: ChIP-seq.repeat.analysis_output_schema.yaml
path: /work/project/becgsc_001/dev/ChIP-seq.repeat.analysis.py/ChIP-seq.repeat.analysis.collator.py
command_template: >
  {pipeline.path} 
  --config {looper.pep_config}
  -O {looper.output_dir}
  -P {compute.cores}
  -M {compute.mem}
  -n {project.name}
  -r {looper.results_subdir}
compute:
  mem: 20G
  cores: 32
  partition: slim16
  time: 8:00:00

